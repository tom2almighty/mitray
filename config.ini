[Mihomo]
; ============================================================================
; Mihomo 核心配置
; ============================================================================

; mihomo 可执行文件的完整路径(必填)
; 示例 Windows 路径:
CorePath=D:\Program\Mihomo\mihomo-windows-amd64.exe
; 或相对路径:
; CorePath=.\mihomo\mihomo.exe

; 本地配置文件路径(ConfigPath 和 ConfigURL 二选一)
; 如果同时配置,ConfigURL 优先级更高
ConfigPath=D:\Program\Mihomo\config.yaml
; 或相对路径:
; ConfigPath=.\config.yaml

; 远程配置文件 URL(可选,订阅链接)
; 如果配置了 URL,会自动下载并使用,优先级高于 ConfigPath
; ConfigURL=https://example.com/clash/config.yaml


[Settings]
; ============================================================================
; 程序设置
; ============================================================================

; 程序启动时自动启动 mihomo 核心
; 1 = 是(推荐),0 = 否
AutoStartCore=1

; 记忆并恢复 TUN 运行时状态（不会修改 mihomo 配置文件）
; 1 = 是,0 = 否
RememberTUN=1

; MiTray 记忆的期望 TUN 状态（托盘开关成功后自动更新）
; 1 = 启用,0 = 禁用
TUNEnabled=0

; mihomo 重启或 WebUI 重载配置后，自动恢复上面的 TUN 状态
; 1 = 是,0 = 否
AutoRestoreTUN=1

; 开机登录后延迟启动秒数（0-600）
AutoStartupDelaySec=15


